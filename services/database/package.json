{
  "name": "sar-database",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm install && npm run start",
    "start": "pouchdb-server -p 5984 -o 0.0.0.0"
  },
  "dependencies": {
    "pouchdb-server": "^4.1.0"
  }
}
